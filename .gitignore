# deck export output
docs/dist/

# throwaway preview of the draft (self-contained, ~2.5MB of inlined mermaid)
draft-preview.html
docs/node_modules/

# scratch space / drafts in progress
tmp/

# editor config stays local
.vscode/

# pipeline services (will adjust per service)
data/
**/__pycache__/
**/.venv/

# local env overrides
.env